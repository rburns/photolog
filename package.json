{
  "scripts": {
    "build": "npm run clean && lein run -m clojure.main script/build.clj",
    "dev": "lein run -m clojure.main script/dev.clj",
    "build-test": "npm run clean && lein run -m clojure.main script/build-test.clj",
    "test": "node ./target/test.js",
    "clean": "rm -rf target"
  },
  "dependencies": {
    "feed": "^1.0.2",
    "sharp": "^0.21.0"
  },
  "devDependencies": {
    "source-map-support": "^0.4.15"
  }
}
